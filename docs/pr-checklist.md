

## How to run

1. Set up the environment:
   - Activate the virtual environment:
     - Windows (Git Bash):
       ```
       source .venv/Scripts/activate
       ```
     - Mac/Linux:
       ```
       source .venv/bin/activate
       ```
   - Install dependencies:
     ```
     pip install -r requirements.txt
     ```

2. Run the code:
   - Navigate to the repo root directory.
   - Execute the script:
     ```
     python scripts/benchmark.py
     ```

3. Confirm success:
   - The program should run without errors.
   - Benchmark timing results should be printed to the terminal.



## Checklist

- [ ] Update documentation
- [ ] Add a feature to the main app screen
- [ ] Fix bugs in app_screen.python
- [ ] Add a README.md file
- [ ] Remove the dark/light mode feature